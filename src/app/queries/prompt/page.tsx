import React from "react";

export default async function Prompt(): Promise<React.JSX.Element> {

    return (
        <div className="min-h-screen flex items-center justify-center">

        </div>)
}